# migration-admin-ui

`migration-admin-ui` 是后端接口迁移平台的管理前端模块，基于 Vue 3 + TypeScript + Element Plus 实现。

## 本地启动

```bash
npm install
npm run dev
```

默认开发地址：`http://localhost:5173`，`/api` 请求会代理到 `http://localhost:8080`。

## 构建

```bash
npm run build
```
