# 环境变量配置示例

# 部署模式：local 或 server
# 可通过命令行参数指定：./build.sh local 或 ./build.sh server
DEPLOY_MODE=local

# 数据库配置
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=migration
MYSQL_USER=migration
MYSQL_PASSWORD=migration

# 应用配置
SPRING_PROFILES_ACTIVE=prod